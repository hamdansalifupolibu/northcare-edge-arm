/**
 * Compatibility re-export.
 * Authoritative Stage 3 tokens live in `src/theme/`.
 * Prefer importing from `../theme` in new code.
 */
export {
  borders,
  colors,
  layout,
  motion,
  opacity,
  radii,
  semanticColors,
  shadows,
  spacing,
  theme,
  typography,
} from '../theme';
