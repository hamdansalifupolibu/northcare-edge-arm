/**
 * Re-export design-system PasswordField for auth feature imports.
 * Prefer importing from `design-system` in new code.
 */
export {
  PasswordField,
  type PasswordFieldProps,
} from '../../../design-system/forms/PasswordField';
