rules_version = '2';

// Demo sync API writes via Cloud Functions Admin SDK only.
// Mobile clients must not read or write Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
