# packages/shared-types

**Purpose:** Future shared TypeScript contracts and documented API schemas.  
**Status:** Placeholder — Stage 1.

## Rules

- TypeScript-only contracts suitable for mobile (and typed API clients).  
- Do not place Python runtime code here.  
- Align with `implementation/route-map.json` and domain models once approved.
