# Reminder conflict policy

Uses Stage 14 conflict framework. Competing schedule changes, handled-versus-active, and cancelled-versus-active require review. Server-authoritative account/facility fields use server state. Device-native scheduling fields are never conflict fields. No silent last-write-wins for reminders.
