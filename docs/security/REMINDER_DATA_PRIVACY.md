# Reminder data privacy

Reminder notes are sensitive and stay inside the authenticated app. They are never placed in native notification title/body/data. Audit events may store reminder ID and status categories only — never client names, notes, or clinical content.
