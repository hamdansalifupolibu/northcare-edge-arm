# Reminder test strategy

Cover permissions, workflow, scheduling/reconciliation, privacy content, tap fail-closed paths, time validation, sync payload exclusion of native IDs, accessibility labels, and development credential provisioning with dynamically generated passwords (never the owner password).
